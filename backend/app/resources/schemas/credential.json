{
  "resource": "credential",
  "table": "credentials",
  "timestamps": true,
  "middleware": ["throttle:100,1"],
  "routes": {
    "list": {"method": "GET", "path": "/api/credential", "middleware": ["auth"]},
    "show": {"method": "GET", "path": "/api/credential/{id}", "middleware": ["auth"]},
    "create": {"method": "POST", "path": "/api/credential", "middleware": ["auth", "json"]},
    "update": {"method": "PUT", "path": "/api/credential/{id}", "middleware": ["auth", "json"]},
    "delete": {"method": "DELETE", "path": "/api/credential/{id}", "middleware": ["auth"]}
  },
  "fields": [
    {
      "name": "user_id",
      "type": "int",
      "required": true
    },
    {
      "name": "name",
      "type": "string",
      "maxLength": 200,
      "required": true
    },
    {
      "name": "type",
      "type": "string",
      "maxLength": 50,
      "required": true
    },
    {
      "name": "config",
      "type": "json",
      "required": false
    }
  ]
}
